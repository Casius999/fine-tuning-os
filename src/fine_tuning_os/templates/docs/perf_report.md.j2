# Performance Report — {{ project_id }}

> Generated by Fine-Tuning OS on {{ generated_at | default("(see file metadata)") }}.

---

## Summary

| Metric | Baseline | Fine-tuned | Delta | Direction |
|--------|----------|------------|-------|-----------|
{% for row in comparison_rows %}
| {{ row.metric }} | {{ row.baseline }} | {{ row.finetuned }} | {{ row.delta }} | {{ row.direction | default("higher=better") }} |
{% endfor %}

---

## Detailed Metrics

### Fine-tuned Model

| Metric | Value |
|--------|-------|
{% for k, v in metrics.items() %}
| {{ k }} | {{ v }} |
{% endfor %}

### Baseline Model

| Metric | Value |
|--------|-------|
{% for k, v in baseline.items() %}
| {{ k }} | {{ v }} |
{% endfor %}

---

## Training Curves

{% if curves_description %}
{{ curves_description }}
{% else %}
Training and validation loss curves are available in the `outputs/` directory.
Export via TensorBoard or Weights & Biases for visual inspection.
{% endif %}

---

## Evaluation Protocol

- **Dataset**: {{ eval_dataset | default("client validation set") }}
- **Split**: {{ eval_split | default("test") }}
- **Metric computation**: hand-rolled BLEU/ROUGE/perplexity (Fine-Tuning OS `compute_metrics`)
- **Date**: {{ eval_date | default("(see project events log)") }}

---

## Notes

{% if notes %}
{% for note in notes %}
- {{ note }}
{% endfor %}
{% else %}
- Metrics are computed on the held-out test split; no data leakage.
- Perplexity reported as `exp(mean NLL)` over the evaluation set.
{% endif %}

---

> **Disclaimer**: This report is generated automatically. Results should be
> interpreted in context of the training data and task definition.
